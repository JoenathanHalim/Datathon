"""
FraudShield – source package.

Modules
-------
data_loader         : Load raw and cleaned CSV datasets.
feature_engineering : Derive new features (age, distance, frequency, job sector).
preprocessing       : Encode, resample (SMOTE), scale, and detect anomalies.
models              : Train and evaluate classification models.
visualization       : Reusable plotting helpers.
"""
